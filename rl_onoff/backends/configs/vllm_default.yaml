backend_type: vllm
model_name: gpt2

# vLLM-specific parameters
backend_specific:
  tensor_parallel_size: null  # Number of GPUs for tensor parallelism (null = 1)
  gpu_memory_utilization: null  # GPU memory utilization ratio (null = 0.9)
  max_model_len: null  # Maximum model length (null = auto)
